import { StyleSheet } from "react-native";
import { themeColors } from "../../../Utils/appTheme";
import { vh, vw } from "../../../Utils/units";

const styles = StyleSheet.create({
    container:{
        flex:1,
        overflow:'hidden',
        elevation:2
    },
})
export default styles